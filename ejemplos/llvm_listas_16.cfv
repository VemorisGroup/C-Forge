funcion sumar_primero(datos: lista): numero {
    retornar datos[0] + datos[1]
}

sea numeros: lista = [2, 3, 4]
numeros.append(5)
mostrar(numeros)
mostrar(numeros.length)
mostrar(sumar_primero(numeros))
destruir(numeros)
